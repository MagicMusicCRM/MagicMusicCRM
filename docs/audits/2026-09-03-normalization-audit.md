# Нормализация и сложность: 03.09.2026

Три подтверждённые находки исправлены в локальном коде: единое отображение денег с копейками, общая визуальная часть диалогов архивирования и удаление неиспользуемых зависимостей календаря. По последнему решению владельца «Расчёты преподавателей» перенесены в самостоятельную вкладку «Аналитики» рядом с «Обзором» и «Журналами».

Поиск охватывал `lib`, `server/src`, тесты и зависимости; затем вручную проверялись конкретные реализации и их вызывающий код. Это проверка сквозных сценариев, а не утверждение, что весь production UI протестирован или каждый исходник прочитан. Старые аудиты не использовались как доказательство текущего поведения. RepoWise overview/context/health/dead-code использовались для навигации; `embedder=mock`, `semantic_search=false` и неполное индексирование требовали проверки результатов по живым исходникам.

## Дополнение: единые окна и значок абонемента

По решению владельца от 03.09.2026 все окна действий используют существующий `magic_sheet.dart`: на Windows/macOS/Linux это центральный диалог с округлением независимо от ширины приложения, на Android/iOS — нижняя выдвижная панель. Web выбирает вид по ширине 840 px. Обычная навигация рабочего пространства, контекстные меню, уведомления и системный выбор файлов сохраняют свою роль.

Убраны отдельный `magic_drawer.dart` и правая панель информации о чате. Редактор занятия больше не открывается отдельной мобильной страницей. Существующие диалоги подтверждения, справочников, доступа, обновления и просмотра вложений переведены на общую точку открытия. Даты, диапазоны и время используют штатные Flutter-компоненты через `magic_picker.dart`, без новой библиотеки или собственного календаря.

Мобильная рамка содержит ручку, разворачивание и крестик, поддерживает закрытие свайпом и учитывает клавиатуру и системную строку. Выход использует `maybePop`, поэтому существующие проверки несохранённого черновика и блокирующей операции сохраняются. Длинные формы прокручиваются внутри ограниченной области. Для полей типа списания и правила оплаты преподавателю исправлено переполнение длинных названий при ограниченной ширине.

Причина отсутствия значка в рабочем общем расписании находится на сервере: deployed `229b8a6f` не проецирует фактический `reservation_state` в matrix, хотя выдаёт его в списке занятий клиента. Локальный `ScheduleReadService.getScheduleMatrix` уже содержит общий SQL-fix: reservation lateral query, вывод из scoped и сериализацию. Добавлены проверки reserved/released/null через реальные PostgreSQL-факты, matrix/exact parity, teacher/stale-role и branch scope, а также badge во всех режимах календаря через API adapter. Дополнительных клиентских запросов, вычисления покрытия по наличию `subscriptionId` и новых правил списания не добавлялось. **Исправление значка требует развёртывания совместимого сервера; одной новой ПК-сборки недостаточно. Production и migration 0146 в этом проходе не изменялись.**

Целевые проверки этого дополнения: backend **2 suites / 31 tests PASS**, 18,897 секунды (`dist/adaptive-modal-backend-badge.log`); сценарии навигации/настроек/расписания **109 PASS** (`dist/adaptive-modal-stale-tests-final.log`); задачи и pickers **39 PASS**, включая полный Android-сценарий с настоящей прокруткой, выбором даты/времени, получателя и напоминания. Исправлены устаревшие тестовые ожидания BackButton/right drawer, неявная Android-платформа настольных тестов и ожидание конца анимации выезда. Предупреждения промаха нажатия не подавлялись. Безопасная область подтверждена отдельным RED→GREEN: развёрнутая рамка раньше начиналась на y=0 при системном отступе 24 px, теперь остаётся ниже него; сохранение и последнее поле доступны с клавиатурой.

Финальная проверка этого дополнения: **1553/1553 Flutter tests PASS**, 5 минут 15 секунд (`dist/adaptive-modal-flutter-final.log`); `flutter analyze --no-pub lib test` — **без замечаний**, 22,5 секунды (`dist/adaptive-modal-analyze-final.log`). Windows Release после последней правки собран за **229,6 секунды** (`dist/adaptive-modal-windows-final.log`). Локальный RepoWise index обновлён, `git diff --check` — PASS. APK/AAB и проверка на физическом телефоне в этот проход не входят; мобильные сценарии проверены Flutter widget-тестами с Android-платформой, реальными жестами и размерами экрана.

Новая ПК-копия: `dist/pc-preview-20260903-123832/magic_music_crm.exe`, AOT SHA-256 `1579752EB44382797D68C926D2F0AF9B76E25572D346D50635055395C3FDD957`. Это локальный preview с прежним build number 209 и production API, не опубликованный release. Для проверки интерфейса можно открыть его отдельно; новая серверная логика финансирования и значка ещё не доступна через старый production backend.

Структура после модального дополнения: Sentrux quality signal **5754** против **5755** у предыдущего локального кандидата (−1), import edges **5955** против **5918** (+37). Граф остаётся ацикличным, depth raw **15** без изменения, обе архитектурные rules проходят; все зависимости DSM направлены вниз. Число файлов в индексе **2764** против **2765**. Это не доказательство общего улучшения метрики. Удалён самостоятельный drawer на 174 строки; существующая рамка получила общую политику и мобильное встраивание штатных диалогов, общие вызывающие зависимости добавились. Сводка: `dist/adaptive-modal-sentrux.json`.

## Исправленные находки исходного прохода

| Приоритет | Что было | Что изменено |
| --- | --- | --- |
| P2 | Один платёж показывался как `1234.56 ₽`, `1 235 ₽` и `1 234,56 ₽` в разных экранах. Стоимость курса, оплата и долг округлялись независимо. | Общий `core/utils/money_format.dart` выводит две десятичные позиции, группирует разряды и сохраняет точность `BigInt`. Формы, карточки клиента и абонемента, финансовые списки и отчёты используют общий formatter. Legacy decimal API получает только presentation bridge; финансовые команды и ledger не менялись. |
| P3 | Три диалога архивирования филиала, аудитории и группы повторяли визуальную часть. | `LifecycleDialogContent` содержит общие поля, историю, загрузку, ошибки и кнопки. Предметные blockers, preview, expected version и команды остались в прежних владельцах. Совокупный объём: 976 → 846 строк, сокращение на 130. |
| P3 | Syncfusion calendar и его локализации оставались в зависимостях после перехода на собственный календарь. | Удалены две прямые и две ставшие ненужными транзитивные зависимости. Удалены импорт/delegate локализаций и устаревший комментарий теста. Версии остальных пакетов сохранены. |
| Решение владельца | Расчёты преподавателей открывались кнопкой в отдельном окне из аналитики. | Третья вкладка «Расчёты преподавателей» показывает существующий отчёт с периодами и начислениями. Выбор периода и данные сохраняются при переключении вкладок. Прежний прямой переход `initialTab: 5` открывает эту вкладку; существующие ограничения доступа сохранены. |

## Проверенные границы и уже изменённые сценарии

| Сценарий | Текущее состояние и доказательство |
| --- | --- |
| Создание и редактирование занятия | Календарь вызывает один `CreateLessonDialog` при создании и редактировании: [schedule_widget_actions.dart:16](<C:/Users/Alinka/Documents/Codex Import/MagicMusicCRM/lib/features/admin/presentation/widgets/schedule_widget_actions.dart:16>) и [schedule_widget_actions.dart:302](<C:/Users/Alinka/Documents/Codex Import/MagicMusicCRM/lib/features/admin/presentation/widgets/schedule_widget_actions.dart:302>). Отмена остаётся отдельной командой. Наличие нескольких backend-команд создания, переноса и коррекции само по себе не считается дублированием: у них разные транзакционные и исторические эффекты. Повторяющиеся маленькие dropdown-компоненты сейчас не основание вводить общий framework форм. |
| Имя и тип клиента | Выбранный клиент и результаты поиска используют имя плюс русское «Лид»/«Ученик»: [lesson_participant_section.dart:103](<C:/Users/Alinka/Documents/Codex Import/MagicMusicCRM/lib/features/admin/presentation/widgets/lesson_editor/lesson_participant_section.dart:103>). Утверждение ограничено проверенным редактором, а не всеми сущностями приложения. |
| Lifecycle и покрытие абонементом | Общая палитра задаёт цвет состояния, покрытие имеет самостоятельный badge: [lesson_state_palette.dart:63](<C:/Users/Alinka/Documents/Codex Import/MagicMusicCRM/lib/core/theme/lesson_state_palette.dart:63>), [lesson_state_badges.dart:86](<C:/Users/Alinka/Documents/Codex Import/MagicMusicCRM/lib/core/widgets/lesson_state_badges.dart:86>). Тесты карточки и календаря подтверждают синий booked с маркером абонемента и зелёный completed без ложного маркера. Обновлены оба противоречивших этому продуктовых абзаца. |
| Повторное открытие финансового решения | Read projection объединяет замороженный состав и фактическое финансирование с решением в порядке correction → последний transition → plan. Текущий mapper очищает несовместимые поля при смене источника оплаты: [crm-mappers.ts:152](<C:/Users/Alinka/Documents/Codex Import/MagicMusicCRM/server/src/crm/crm-mappers.ts:152>). Проверены фактический плательщик, sparse group decisions, нормализация скидки, отсутствие внутренних rate snapshots и редактирование абонемента после личного счёта. |
| История расчётов и русские названия | Исправлены сырые rule keys и выбор действующей записи: история учитывает последний transition и последующие корректировки, возвращает названия из сохранённой ревизии справочника. Изменение текущего справочника не переименовывает исторический расчёт. Подтверждено PostgreSQL-регрессиями в `completion-worker-postgres.integration.spec.ts` и `reschedule-postgres.integration.spec.ts`, а также Flutter-тестом `lesson_link_affordance_test.dart`. Предполагаемый вывод `expired` в карточке абонементов исключён из находок: актуальный [client_card_loaders.dart:84](<C:/Users/Alinka/Documents/Codex Import/MagicMusicCRM/lib/features/crm/presentation/client_card/client_card_loaders.dart:84>) оставляет только active. Названия сохранённых фактов в lifecycle-диалогах уже локализованы; ключи `payments`/`lessons` прямо не выводятся. |

## Проверки этого прохода

Дополнительный пакет с вкладкой аналитики, деньгами, lifecycle и зависимостями: `flutter analyze --no-pub lib test` — без замечаний. Полный Flutter-прогон завершился за 3 минуты 31 секунду: **1534 PASS, один FAIL** в `reporting_views_test.dart`, где оставалось ожидание старого разделителя перед знаком валюты. После замены только тестового ожидания повторно прошёл весь затронутый файл: **11/11 PASS**. Код приложения после полного прогона не менялся. Полный набор повторно не запускался. Логи: `dist/normalization-followup-flutter.log`, `dist/normalization-followup-reporting-recheck.log`, `dist/normalization-followup-analyze.log`.

Windows Release собран за **147,3 секунды**; артефакт — `build/windows/x64/runner/Release/magic_music_crm.exe`, AOT — `data/app.so`. Лог: `dist/normalization-followup-windows.log`. Локальный индекс RepoWise обновлён, `git diff --check` прошёл. Сборка не публиковалась и не подменяла запущенную ПК-версию; проверка нового клиента на production не выполнялась. Для новых сценариев финансирования нужен совместимый сервер с migration 0146, которая ещё не развёрнута.

Целевые проверки: 15 тестов навигации и realtime UI, 62 теста денег/форм/отчётов, 6 существующих тестов lifecycle, 18 тестов календаря/startup — PASS. Проверены сохранение периода третьей вкладки, прежний прямой переход, запрет скрытого запроса при потере доступа, одинаковое отображение `±123456` minor в трёх представлениях и точность больших `BigInt`.

До дополнительного пакета полный Flutter-прогон содержал 1529 PASS; его логи `dist/lesson-normalization-flutter-final.log`, `dist/lesson-normalization-analyze.log`, `dist/lesson-normalization-windows-final.log` относятся только к предыдущему локальному кандидату.

Backend не менялся в дополнительном пакете. Для текущих backend-исходников ранее получено: **279 наборов / 3689 тестов, все прошли**, 186,95 секунды, typecheck — PASS. Проверены реальные PostgreSQL-факты оплаты другим учеником, в том числе за занятие лида, корректировки и переносы, права доступа, автоматическое завершение и история названий справочника. Это локальная проверка; migration 0146 и новый сервер не выкладывались в production.

Финансовая read projection: 53 теста из трёх backend-наборов прошли, включая изолированный сценарий на локальном PostgreSQL с rollback. После дополнительного исправления personal account → subscription повторно прошли 46 unit-тестов mapper и schedule read; это не повторный запуск PostgreSQL. Backend typecheck повторно прошёл после последней правки. Flutter legacy adapter — 7 тестов; палитра, ширина карточек и режимы календаря — 20 тестов; отдельный reserved coverage — 1 тест. Targeted analyze изменённых файлов палитры/представлений/adapter прошёл. Полный release gate в этот отчёт не входит.

Редактор после завершающего рефакторинга: `create_lesson_dialog.dart` — 315 строк, архитектурная граница `<320` и запрет владения состоянием/навигацией в чистых UI-компонентах проходят. Exact hydration и разрешение плательщика находятся в существующем DataController; подписанный preview, подтверждение и восстановление после stale version — в SaveFlow; UI получает существующий интерфейс контроллера. Загрузка справочников блокирует только сохранение и только до завершения последнего выбора. Повторный запрос уже загруженных абонементов устранён; отсутствующий исторический выбранный абонемент догружается, а посторонний исчерпанный не выбирается по умолчанию. Целевой прогон архитектуры, контроллеров и UI — 67 PASS; последующий прогон UI с новыми регрессиями и полным `lesson_form_test.dart` — 64 PASS. Анализ изменённых исходников и тестов чистый.

## Sentrux: текущее качество структуры

Источник — [сохранённая сводка живого запуска](<C:/Users/Alinka/Documents/Codex Import/MagicMusicCRM/dist/lesson-normalization-sentrux.json>). Это локальный игнорируемый артефакт; постоянный baseline session Sentrux не сохранялся.

| Показатель | До изменений | Текущий запуск | Изменение |
| --- | ---: | ---: | ---: |
| Quality signal | 5807 | 5755 | −52 |
| Файлы | 2765 | 2765 | 0 |
| Строки | 588894 | 590939 | +2045 |
| Import edges | 5901 | 5918 | +17 |

Относительно исходного baseline метрика немного снизилась; доказательств общего улучшения оценки нет. По сравнению с предыдущим проходом до трёх дополнительных исправлений quality signal не изменился (5755), а число строк в индексе снизилось на 193. Текущий граф ацикличен: acyclicity raw 0 / score 10000, все 5918 зависимостей DSM направлены вниз, две проверенные rules прошли без нарушений. Максимальная depth raw 15 / score 3478; equality score 6512, modularity 5584, redundancy 4991. Повторения UI и общая сложность оцениваются отдельно.

В живом коде появились дополнительные зависимости редактора от `lesson_client_funding_fields` и settlement-компонентов от `lesson-funding`/`commercial-price`. Последние разделяют одну реализацию цен между оплатой занятия и коммерческими условиями абонемента; это обоснованная общая точка. Новые функции и регрессии объясняют рост кода как возможный фактор оценки, но без сопоставимого пофайлового графа нельзя приписать все −52 конкретному файлу или helper. Неизменное число файлов не доказывает включение либо исключение untracked-файлов. Подгонка метрики удалением нужных проверок или склеиванием модулей не предлагается.

## Ponytail: выполненное сокращение

1. Общая визуальная часть lifecycle-диалогов: **976 → 846 строк, −130**. Предметные команды, preview, version и blockers сохранены.
2. Неиспользуемый Syncfusion: **−36 строк** в `pubspec.yaml`, lockfile и `main.dart`, **−2 прямые / −2 транзитивные зависимости**. Другие версии пакетов не изменились; устаревший комментарий теста удалён отдельно.
3. Переход от кнопки/модального окна к третьей вкладке аналитики и сохранению её состояния: **−14 строк** в двух исходниках. Существующий отчёт используется напрямую.
4. Денежные formatter сведены к `money_format.dart`, включая tooltip ленты лида. Это исправление точности и единообразия, а не заявленная экономия строк. Форматтер не восстанавливает точность, уже потерянную в legacy `double`; прежняя пропорциональная формула остатка абонемента не менялась. Пути с minor units сохраняют `BigInt`.

Предыдущая приблизительная оценка «−300 строк» заменена фактическими ограниченными результатами. Общая метрика Sentrux не подменяет проверку пользовательских сценариев.
